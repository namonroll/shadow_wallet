enum UserRole { parent, child }

enum TaskCategory { A, B, C, D }

enum TaskDayType { weekday, weekend }

enum TaskStatus { completed, uncompleted, reDiscuss }

enum AccountType { single, double }

enum OverrideType { reward, deduction, reExecute }

enum WalletType { pocketMoney, savings }
